# Upstream master plus patches/common, for every architecture.
PATCH_DIR=patches/common
CC=gcc-14
CXX=g++-14
TUNE=""
# prebuilt: LizardByte/build-deps release, which only covers x86_64 and aarch64.
# Other architectures fall back to source.
FFMPEG=prebuilt
